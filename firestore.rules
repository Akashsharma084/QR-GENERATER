rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for OmniQR free spark plan sharing
    match /omni_qrs/{qrId} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
